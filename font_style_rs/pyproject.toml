[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "font-style-rs"
version = "0.1.0"
requires-python = ">=3.8"
description = "Fast italic/bold font-style analyzer for comic-typesetting (Rust extension)."

[tool.maturin]
module-name = "font_style_rs"
